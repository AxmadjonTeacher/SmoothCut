{
  "name": "smoothcut",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.27.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter @smoothcut/desktop dev",
    "build": "turbo run build",
    "build:native": "turbo run build:native",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0",
    "turbo": "^2.10.5",
    "typescript": "~5.9.3"
  }
}
